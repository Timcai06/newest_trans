# 组件文档

## 一、组件概述

本项目采用组件化开发模式，将现有直接DOM操作代码转换为组件化开发，提高代码复用性和可维护性，统一UI设计风格，支持浅色/深色主题切换。

## 二、组件目录结构

```
components/
├── base/                     # 基础组件
├── complex/                  # 复合组件
├── business/                 # 业务组件
├── layouts/                  # 布局组件
├── content/                  # Content脚本组件
├── background/               # Background脚本组件
├── utils/                    # 组件工具
├── styles/                   # 组件样式
└── index.js                  # 组件总出口
```

## 三、组件工具

### 3.1 BaseComponent

**组件描述**：组件基类，所有组件都继承自该类，提供组件的基础功能，包括props管理、样式注册、事件处理等。

**核心方法**：
- `constructor(props)`：构造函数，初始化组件属性和状态
- `init()`：初始化组件
- `registerStyle()`：注册组件样式
- `setState(newState, callback)`：设置组件状态
- `bindEvent(element, eventName, callback)`：绑定事件
- `unbindEvents()`：解绑所有事件
- `render()`：渲染组件（子类必须重写）
- `update(newProps)`：更新组件
- `destroy()`：销毁组件

**使用示例**：
```javascript
class MyComponent extends BaseComponent {
  constructor(props) {
    super(props);
  }
  
  render() {
    // 组件渲染逻辑
  }
}
```

### 3.2 ComponentRegistry

**组件描述**：组件注册器，用于管理所有组件的注册和创建。

**核心方法**：
- `register(componentName, componentClass)`：注册组件
- `create(componentName, props)`：创建组件实例
- `getComponent(componentName)`：获取组件类

**使用示例**：
```javascript
ComponentRegistry.register('Button', Button);
const button = ComponentRegistry.create('Button', { text: 'Click me' });
```

### 3.3 StyleManager

**组件描述**：样式管理器，用于管理组件的样式注入和主题切换。

**核心方法**：
- `init()`：初始化样式管理器
- `registerStyle(componentName, css)`：注册组件样式
- `setTheme(theme)`：切换主题
- `getCurrentTheme()`：获取当前主题

**使用示例**：
```javascript
StyleManager.registerStyle('Button', '.button { padding: 8px 16px; }');
StyleManager.setTheme('dark');
```

### 3.4 EventBus

**组件描述**：事件总线，用于组件间的通信。

**核心方法**：
- `on(eventName, callback)`：监听事件
- `emit(eventName, data)`：触发事件
- `off(eventName, callback)`：取消监听

**使用示例**：
```javascript
EventBus.on('themeChange', (theme) => {
  console.log('Theme changed to:', theme);
});

EventBus.emit('themeChange', 'dark');
```

## 四、基础组件

### 4.1 Button

**组件描述**：按钮组件，支持多种样式变体、尺寸和图标。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| text | string | '' | 按钮文本 |
| type | string | 'primary' | 按钮类型（primary, secondary, danger, success） |
| size | string | 'medium' | 按钮尺寸（small, medium, large） |
| disabled | boolean | false | 是否禁用 |
| onClick | function | null | 点击事件回调 |
| icon | string | '' | 图标类名 |

**使用示例**：
```javascript
const button = new Button({
  text: '点击我',
  type: 'primary',
  size: 'medium',
  onClick: () => {
    console.log('Button clicked');
  }
});
```

### 4.2 Card

**组件描述**：卡片组件，支持不同卡片类型和流体边框动画。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| title | string | '' | 卡片标题 |
| content | string | '' | 卡片内容 |
| type | string | 'default' | 卡片类型（default, word, stat） |
| fluidBorder | boolean | false | 是否启用流体边框动画 |
| className | string | '' | 额外CSS类名 |

**使用示例**：
```javascript
const card = new Card({
  title: '示例卡片',
  content: '这是卡片内容',
  type: 'default'
});
```

### 4.3 Input

**组件描述**：输入框组件，支持不同输入类型、聚焦状态和错误状态。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| type | string | 'text' | 输入类型 |
| placeholder | string | '' | 占位符文本 |
| value | string | '' | 输入值 |
| disabled | boolean | false | 是否禁用 |
| error | boolean | false | 是否显示错误状态 |
| onChange | function | null | 输入变化事件回调 |
| onFocus | function | null | 聚焦事件回调 |
| onBlur | function | null | 失焦事件回调 |

**使用示例**：
```javascript
const input = new Input({
  type: 'text',
  placeholder: '请输入内容',
  onChange: (value) => {
    console.log('Input value:', value);
  }
});
```

### 4.4 Dialog

**组件描述**：弹窗组件，支持不同弹窗类型、动画效果和主题切换。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| title | string | '' | 弹窗标题 |
| content | string | '' | 弹窗内容 |
| type | string | 'default' | 弹窗类型（default, confirm, alert） |
| visible | boolean | false | 是否可见 |
| onClose | function | null | 关闭事件回调 |
| onConfirm | function | null | 确认事件回调 |
| onCancel | function | null | 取消事件回调 |

**使用示例**：
```javascript
const dialog = new Dialog({
  title: '确认弹窗',
  content: '确定要执行此操作吗？',
  type: 'confirm',
  visible: true,
  onConfirm: () => {
    console.log('Confirm clicked');
  },
  onCancel: () => {
    console.log('Cancel clicked');
  }
});
```

### 4.5 Toast

**组件描述**：提示组件，支持不同提示类型、自动关闭和主题切换。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| message | string | '' | 提示消息 |
| type | string | 'info' | 提示类型（info, success, error, warning） |
| duration | number | 3000 | 自动关闭时间（毫秒） |
| onClose | function | null | 关闭事件回调 |

**使用示例**：
```javascript
const toast = new Toast({
  message: '操作成功',
  type: 'success',
  duration: 3000
});
```

## 五、复合组件

### 5.1 WordCard

**组件描述**：翻译记录卡片组件，基于Card组件开发，支持流体边框动画和收藏功能。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| word | string | '' | 单词 |
| translation | string | '' | 翻译 |
| phonetic | string | '' | 音标 |
| example | string | '' | 例句 |
| isFavorite | boolean | false | 是否收藏 |
| onFavoriteToggle | function | null | 收藏切换事件回调 |
| onDelete | function | null | 删除事件回调 |

**使用示例**：
```javascript
const wordCard = new WordCard({
  word: 'example',
  translation: '例子',
  phonetic: '/ɪɡˈzɑːmpl/',
  isFavorite: false,
  onFavoriteToggle: (isFavorite) => {
    console.log('Favorite toggled:', isFavorite);
  }
});
```

### 5.2 StatCard

**组件描述**：统计卡片组件，基于Card组件开发，用于展示统计数据和趋势。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| title | string | '' | 统计标题 |
| value | string | '' | 统计值 |
| unit | string | '' | 单位 |
| trend | number | 0 | 趋势值（正数为增长，负数为下降） |
| icon | string | '' | 图标类名 |

**使用示例**：
```javascript
const statCard = new StatCard({
  title: '已翻译单词',
  value: '1,234',
  unit: '个',
  trend: 15,
  icon: 'icon-word'
});
```

### 5.3 LearningPanel

**组件描述**：学习面板组件，基于Button组件开发，用于切换不同的学习模式。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| modes | array | [] | 学习模式列表 |
| activeMode | string | '' | 当前激活的模式 |
| onModeChange | function | null | 模式切换事件回调 |

**使用示例**：
```javascript
const learningPanel = new LearningPanel({
  modes: [
    { id: 'word', name: '单词模式' },
    { id: 'sentence', name: '句子模式' },
    { id: 'listening', name: '听力模式' }
  ],
  activeMode: 'word',
  onModeChange: (mode) => {
    console.log('Mode changed to:', mode);
  }
});
```

### 5.4 SearchBar

**组件描述**：搜索栏组件，基于Input和Button组件开发，支持搜索功能、搜索历史和建议。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| placeholder | string | '' | 搜索占位符 |
| onSearch | function | null | 搜索事件回调 |
| onInput | function | null | 输入变化事件回调 |
| showHistory | boolean | true | 是否显示搜索历史 |
| showSuggestions | boolean | true | 是否显示搜索建议 |

**使用示例**：
```javascript
const searchBar = new SearchBar({
  placeholder: '搜索单词或句子',
  onSearch: (keyword) => {
    console.log('Searching for:', keyword);
  }
});
```

### 5.5 SettingsPanel

**组件描述**：设置面板组件，用于管理应用的各项设置，包括外观、AI翻译、常规翻译、学习设置和数据管理等。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| settings | object | {} | 当前设置 |
| onSettingChange | function | null | 设置变化事件回调 |

**使用示例**：
```javascript
const settingsPanel = new SettingsPanel({
  settings: {
    theme: 'light',
    aiTranslation: true,
    autoTranslate: true
  },
  onSettingChange: (settingName, value) => {
    console.log(`${settingName} changed to:`, value);
  }
});
```

## 六、业务组件

### 6.1 TranslationPopup

**组件描述**：翻译弹窗组件，用于显示单词翻译结果，包括音标、翻译、例句等。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| translation | object | null | 翻译结果对象 |
| position | object | { x: 0, y: 0 } | 弹窗位置 |
| onClose | function | null | 关闭事件回调 |
| onFavorite | function | null | 收藏事件回调 |

**使用示例**：
```javascript
const translationPopup = new TranslationPopup({
  translation: {
    word: 'example',
    translation: '例子',
    phonetic: '/ɪɡˈzɑːmpl/'
  },
  position: { x: 100, y: 100 }
});
```

## 七、布局组件

### 7.1 Header

**组件描述**：头部组件，包含logo、标题、设置按钮等，支持主题切换。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| title | string | '' | 头部标题 |
| showLogo | boolean | true | 是否显示logo |
| showSettings | boolean | true | 是否显示设置按钮 |
| onSettingsClick | function | null | 设置按钮点击事件回调 |

**使用示例**：
```javascript
const header = new Header({
  title: '单词翻译助手',
  showLogo: true,
  showSettings: true,
  onSettingsClick: () => {
    console.log('Settings clicked');
  }
});
```

### 7.2 Content

**组件描述**：内容区域组件，支持不同布局类型和内容类型，适配主题系统。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| children | array | [] | 子组件列表 |
| layout | string | 'default' | 布局类型（default, grid, flex） |
| className | string | '' | 额外CSS类名 |

**使用示例**：
```javascript
const content = new Content({
  children: [wordCard, statCard],
  layout: 'grid',
  className: 'main-content'
});
```

### 7.3 DashboardLayout

**组件描述**：Dashboard布局组件，包含侧边栏、头部和主内容区，支持响应式设计和主题切换。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| title | string | '' | Dashboard标题 |
| showSidebar | boolean | true | 是否显示侧边栏 |
| initialSection | string | 'overview' | 初始显示的内容区域 |
| sections | array | [] | 内容区域配置 |

**使用示例**：
```javascript
const dashboardLayout = new DashboardLayout({
  title: '学习Dashboard',
  showSidebar: true,
  initialSection: 'overview',
  sections: [
    { id: 'overview', name: '学习概览', icon: '📊' },
    { id: 'records', name: '学习记录', icon: '📝' },
    { id: 'modes', name: '学习模式', icon: '🎯' }
  ]
});
```

## 八、Dashboard组件

### 8.1 StatsCard

**组件描述**：Dashboard统计卡片组件，用于展示学习概览数据，包括今日学习、掌握程度、待复习等。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| title | string | '' | 统计标题 |
| value | number | 0 | 统计值 |
| icon | string | '' | 统计图标（emoji） |
| subtitle | string | '' | 统计副标题 |
| progress | number | 0 | 进度百分比（0-100） |

**使用示例**：
```javascript
const statsCard = new StatsCard({
  title: '今日学习',
  value: 15,
  icon: '📚',
  subtitle: '目标 20 词',
  progress: 75
});
```

### 8.2 DashboardManager

**组件描述**：Dashboard数据管理组件，负责管理学习数据、统计计算和学习模式。

**核心方法**：
- `getLearningOverview()`：获取学习概览数据
- `getLearningRecords(filterOptions)`：获取学习记录
- `getLearningStats(timeRange)`：获取学习统计数据
- `setLearningGoal(goal)`：设置学习目标
- `startLearningMode(modeId, options)`：开始学习模式
- `getLearningModes()`：获取所有学习模式

**使用示例**：
```javascript
const dashboardManager = new DashboardManager();
const overview = await dashboardManager.getLearningOverview();
console.log('学习概览:', overview);
```

### 8.3 LineChart

**组件描述**：折线图组件，用于展示学习趋势和统计数据。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| data | object | {} | 图表数据 |
| options | object | {} | 图表配置选项 |
| width | number | 400 | 图表宽度 |
| height | number | 300 | 图表高度 |

**使用示例**：
```javascript
const lineChart = new LineChart({
  data: {
    labels: ['周一', '周二', '周三', '周四', '周五', '周六', '周日'],
    datasets: [{
      label: '学习单词数',
      data: [15, 20, 18, 25, 22, 30, 28],
      borderColor: '#667eea'
    }]
  },
  width: 400,
  height: 300
});
```

### 8.4 BarChart

**组件描述**：柱状图组件，用于展示不同学习模式的使用情况和统计数据。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| data | object | {} | 图表数据 |
| options | object | {} | 图表配置选项 |
| width | number | 400 | 图表宽度 |
| height | number | 300 | 图表高度 |

**使用示例**：
```javascript
const barChart = new BarChart({
  data: {
    labels: ['闪卡模式', '测验模式', '拼写模式', '听力模式'],
    datasets: [{
      label: '使用次数',
      data: [150, 80, 60, 40],
      backgroundColor: '#667eea'
    }]
  },
  width: 400,
  height: 300
});
```

### 8.5 PieChart

**组件描述**：饼图组件，用于展示学习数据的分布情况，如难度分布、掌握程度分布等。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| data | object | {} | 图表数据 |
| options | object | {} | 图表配置选项 |
| width | number | 400 | 图表宽度 |
| height | number | 300 | 图表高度 |

**使用示例**：
```javascript
const pieChart = new PieChart({
  data: {
    labels: ['简单', '中等', '困难'],
    datasets: [{
      data: [45, 35, 20],
      backgroundColor: ['#48bb78', '#ed8936', '#f56565']
    }]
  },
  width: 400,
  height: 300
});
```

## 八、Content脚本组件

### 8.1 TextSelection

**组件描述**：文本选择组件，用于处理网页中的文本选择和翻译触发。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| onTextSelected | function | null | 文本选择事件回调 |
| minSelectionLength | number | 1 | 最小选择长度 |
| maxSelectionLength | number | 100 | 最大选择长度 |

**使用示例**：
```javascript
const textSelection = new TextSelection({
  onTextSelected: (text, selection) => {
    console.log('Text selected:', text);
  },
  minSelectionLength: 1,
  maxSelectionLength: 100
});
```

### 8.2 TranslationRequest

**组件描述**：翻译请求组件，用于处理翻译请求的发送和管理。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| onTranslationComplete | function | null | 翻译完成事件回调 |
| onTranslationError | function | null | 翻译错误事件回调 |

**使用示例**：
```javascript
const translationRequest = new TranslationRequest({
  onTranslationComplete: (result) => {
    console.log('Translation complete:', result);
  },
  onTranslationError: (error) => {
    console.error('Translation error:', error);
  }
});
```

### 8.3 TranslationHighlight

**组件描述**：翻译结果高亮组件，用于高亮显示已翻译的单词和词组。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| translations | array | [] | 翻译结果列表 |
| highlightClass | string | 'translation-highlight' | 高亮CSS类名 |

**使用示例**：
```javascript
const translationHighlight = new TranslationHighlight({
  translations: [{ word: 'example', translation: '例子' }],
  highlightClass: 'translation-highlight'
});
```

### 8.4 TranslationFloatingPopup

**组件描述**：悬浮翻译弹窗组件，用于在网页中显示翻译结果。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| translation | object | null | 翻译结果对象 |
| position | object | { x: 0, y: 0 } | 弹窗位置 |
| onClose | function | null | 关闭事件回调 |

**使用示例**：
```javascript
const floatingPopup = new TranslationFloatingPopup({
  translation: {
    word: 'example',
    translation: '例子'
  },
  position: { x: 100, y: 100 }
});
```

### 8.5 ContextMenu

**组件描述**：上下文菜单和快捷键组件，用于处理右键菜单和键盘快捷键。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| menuItems | array | [] | 菜单项列表 |
| shortcuts | object | {} | 快捷键配置 |
| onMenuItemClick | function | null | 菜单项点击事件回调 |
| onShortcut | function | null | 快捷键触发事件回调 |

**使用示例**：
```javascript
const contextMenu = new ContextMenu({
  menuItems: [
    { id: 'translate', name: '翻译', shortcut: 'Ctrl+T' },
    { id: 'addToDictionary', name: '添加到词典' }
  ],
  onMenuItemClick: (itemId) => {
    console.log('Menu item clicked:', itemId);
  }
});
```

## 九、Background脚本组件

### 9.1 BackgroundAPIRequest

**组件描述**：Background API请求处理组件，用于处理翻译API请求。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| defaultYoudaoAppKey | string | '' | 默认有道API密钥 |
| defaultYoudaoAppSecret | string | '' | 默认有道API密钥 |
| youdaoApiUrl | string | '' | 有道API地址 |

**使用示例**：
```javascript
const apiRequest = new BackgroundAPIRequest({
  defaultYoudaoAppKey: 'your-app-key',
  defaultYoudaoAppSecret: 'your-app-secret'
});
```

### 9.2 BackgroundMessage

**组件描述**：Background消息传递组件，用于处理扩展内部消息通信。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| apiRequestComponent | object | null | API请求组件实例 |

**使用示例**：
```javascript
const messageComponent = new BackgroundMessage({
  apiRequestComponent: apiRequest
});
```

### 9.3 BackgroundDataSync

**组件描述**：Background数据同步组件，用于处理数据同步和存储变化监听。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| syncInterval | number | 3600000 | 同步间隔（毫秒） |

**使用示例**：
```javascript
const dataSync = new BackgroundDataSync({
  syncInterval: 3600000 // 1小时
});
```

### 9.4 BackgroundScheduler

**组件描述**：Background定时任务组件，用于管理定时任务的注册、取消、暂停、恢复和执行。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| tasks | array | [] | 初始任务列表 |

**使用示例**：
```javascript
const scheduler = new BackgroundScheduler({
  tasks: [
    {
      id: 'daily-backup',
      name: '每日备份',
      cron: '0 0 * * *', // 每天凌晨执行
      callback: () => {
        console.log('Daily backup executed');
      }
    }
  ]
});
```

### 9.5 BackgroundStateManager

**组件描述**：Background扩展状态管理组件，用于管理扩展的状态和服务状态。

**Props**：
| 属性名 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| initialState | object | {} | 初始状态 |

**使用示例**：
```javascript
const stateManager = new BackgroundStateManager({
  initialState: {
    isActive: true,
    apiStatus: 'available'
  }
});
```
